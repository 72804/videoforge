[project]
name = "videoforge-api"
version = "0.1.0"
description = "Thin Vercel Services root for the VideoForge FastAPI app."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
]

[tool.vercel]
entrypoint = "app:app"
